{
  "scripts": {
    "build": "dotnet build --configuration Release",
    "test": "cd CP_Test && dotnet test --configuration Release",
    "ci": "dotnet r build &&  dotnet r test"  ,
    "pack":"dotnet r ci  && dotnet pack -o ../nugetPackages"
  
  }
}